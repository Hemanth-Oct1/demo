name: Deploy MuleSoft Application
 
on:
  push:
    branches:
      - demo-g
  pull_request:
    branches:
      - demo-g
      
jobs:
  call-workflow:
    uses: Hemanth-Oct1/demo/.github/workflows/reusableWorkflow.yml@demo-g
    secrets: inherit
